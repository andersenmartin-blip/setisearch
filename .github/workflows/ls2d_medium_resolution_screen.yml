name: LS2D HD 260655 light-sail leakage screen

on:
  workflow_dispatch:
  push:
    branches: [ls2d-hd260655-screen]
    paths:
      - ".github/workflows/ls2d_medium_resolution_screen.yml"
      - "LS2D_HD260655_PREREGISTRATION.md"
      - "LS2D_FREEZE.sha256"
      - "config/ls2d_hd260655_light_sail.json"
      - "scripts/ls2d_*.py"
      - "tests/test_ls2d_light_sail.py"

permissions:
  contents: write

jobs:
  screen:
    runs-on: ubuntu-24.04
    timeout-minutes: 180

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Require unpublished LS2D result boundary
        run: |
          test ! -e results_ls2d/screen.json
          test ! -e LS2D_STAGE1_RESULT.md
          test ! -e DATA_MANIFEST_LS2D.sha256
          test ! -e RESULTS_MANIFEST_LS2D.sha256
          test ! -e LS2D_SCREEN_PROVENANCE.json

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install detector and HDF5 dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e . \
            'h5py>=3.11,<4' \
            'hdf5plugin>=5,<7' \
            'pytest>=8,<10'

      - name: Verify prospective freeze and inherited detector
        run: |
          sha256sum -c LS2D_FREEZE.sha256
          python -m pytest -q tests/test_ls2d_light_sail.py
          python scripts/ls2d_synthetic_validation.py
          test "$(jq -r .recovered results_ls2d/synthetic_validation.json)" = "true"
          test "$(jq -r .spectral_dataset_values_read results_ls2d/synthetic_validation.json)" = "false"

      - name: Fetch identity-bound medium-resolution products
        run: |
          python scripts/ls2d_fetch.py config/ls2d_hd260655_light_sail.json
          test "$(wc -l < DATA_MANIFEST_LS2D.sha256)" -eq 6
          sha256sum -c DATA_MANIFEST_LS2D.sha256

      - name: Execute frozen ABACAD broadband screen
        run: |
          python scripts/ls2d_screen.py config/ls2d_hd260655_light_sail.json
          python scripts/ls2d_result_summary.py
          test "$(jq -r .spectral_dataset_values_read results_ls2d/screen.json)" = "true"
          test "$(jq -r .raw_spectral_payload_published results_ls2d/screen.json)" = "false"
          test "$(jq -r .technosignature_claimed results_ls2d/screen.json)" = "false"
          test "$(jq -r .status results_ls2d/screen.json)" != "invalid-retention-truncated"

      - name: Record reproducibility evidence
        run: |
          python -m pip freeze > results_ls2d/environment.txt
          python - <<'PY'
          import json
          import os
          from pathlib import Path

          Path('results_ls2d/run_metadata.json').write_text(
              json.dumps(
                  {
                      'execution_commit': os.environ['GITHUB_SHA'],
                      'github_repository': os.environ['GITHUB_REPOSITORY'],
                      'github_run_id': int(os.environ['GITHUB_RUN_ID']),
                      'branch': os.environ['GITHUB_REF_NAME'],
                      'raw_spectral_payload_published': False,
                      'technosignature_claimed': False,
                  },
                  indent=2,
                  sort_keys=True,
              ) + '\n',
              encoding='utf-8',
          )
          PY
          find results_ls2d -type f -print0 \
            | sort -z \
            | xargs -0 sha256sum > RESULTS_MANIFEST_LS2D.sha256
          sha256sum -c RESULTS_MANIFEST_LS2D.sha256

      - name: Upload derived evidence only
        id: evidence
        uses: actions/upload-artifact@v4
        with:
          name: ls2d-medium-resolution-screen
          if-no-files-found: error
          retention-days: 90
          path: |
            DATA_MANIFEST_LS2D.sha256
            LS2D_STAGE1_RESULT.md
            RESULTS_MANIFEST_LS2D.sha256
            results_ls2d/

      - name: Publish checked derived result atomically
        env:
          ARTIFACT_ID: ${{ steps.evidence.outputs.artifact-id }}
          ARTIFACT_DIGEST: ${{ steps.evidence.outputs.artifact-digest }}
        run: |
          python - <<'PY'
          import json
          import os
          from pathlib import Path

          Path('LS2D_SCREEN_PROVENANCE.json').write_text(
              json.dumps(
                  {
                      'artifact_digest': os.environ['ARTIFACT_DIGEST'],
                      'artifact_id': int(os.environ['ARTIFACT_ID']),
                      'artifact_name': 'ls2d-medium-resolution-screen',
                      'execution_commit': os.environ['GITHUB_SHA'],
                      'github_run_id': int(os.environ['GITHUB_RUN_ID']),
                      'raw_spectral_payload_published': False,
                  },
                  indent=2,
                  sort_keys=True,
              ) + '\n',
              encoding='utf-8',
          )
          PY
          sha256sum -c DATA_MANIFEST_LS2D.sha256
          sha256sum -c RESULTS_MANIFEST_LS2D.sha256
          git fetch origin ls2d-hd260655-screen
          test "$(git rev-parse origin/ls2d-hd260655-screen)" = "$GITHUB_SHA"
          git add DATA_MANIFEST_LS2D.sha256 LS2D_STAGE1_RESULT.md \
            LS2D_SCREEN_PROVENANCE.json RESULTS_MANIFEST_LS2D.sha256 \
            results_ls2d
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git commit -m "Publish LS2D medium-resolution screen"
          git push origin HEAD:ls2d-hd260655-screen
