name: LS8AJ EC13080-1508 paired-image metadata preflight
on:
  push:
    branches: [m43-support-qualification]
    paths: ['.github/workflows/ls8aj_metadata.yml']
permissions:
  contents: write
concurrency:
  group: ls8aj-metadata
  cancel-in-progress: false
jobs:
  metadata:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    env:
      PYTHONPATH: src
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12.14'
      - run: python -m pip install numpy==2.3.5 astropy==7.0.2
      - name: Read metadata, join every context, independently audit
        id: metadata
        continue-on-error: true
        shell: bash
        run: |
          set -euo pipefail
          python -u scripts/ls8aj_metadata.py 2>&1 | tee /tmp/ls8aj_metadata.log
          python -u scripts/ls8aj_join_metadata.py 2>&1 | tee /tmp/ls8aj_joins.log
          python -u scripts/ls8aj_images_audit.py --metadata-only 2>&1 | tee /tmp/ls8aj_meta_audit.log
      - name: Preserve metadata outcome and exact future ranges
        if: always()
        env:
          RUN_OUTCOME: ${{ steps.metadata.outcome }}
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p results_ls8aj_metadata
          for f in /tmp/ls8aj_metadata.log /tmp/ls8aj_joins.log /tmp/ls8aj_meta_audit.log; do
            if test -f "$f"; then cp "$f" results_ls8aj_metadata/; fi
          done
          python -m pip freeze | sort > results_ls8aj_metadata/environment.txt
          python - <<'PY'
          import hashlib,json,os
          from pathlib import Path
          out=Path('results_ls8aj_metadata'); audit=out/'audit.json'
          status=json.loads(audit.read_text())['status'] if audit.exists() else 'INCOMPLETE'
          (out/'RUN_STATUS.json').write_text(json.dumps({'execution_outcome':os.environ['RUN_OUTCOME'],'scientific_status':status,'freeze_commit':os.environ['GITHUB_SHA']},indent=2)+'\n')
          (out/'SHA256SUMS').write_text(''.join(f'{hashlib.sha256(p.read_bytes()).hexdigest()}  {p.relative_to(out)}\n' for p in sorted(out.rglob('*')) if p.is_file() and p.name!='SHA256SUMS'))
          PY
          git add results_ls8aj_metadata
          if test -f config/ls8aj_images.json; then git add config/ls8aj_images.json; fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git commit -m 'Publish LS8AJ image metadata joins and exact future payload ranges'
          git push origin HEAD:m43-support-qualification
      - name: Require successful metadata and join gate
        if: always()
        env:
          RUN_OUTCOME: ${{ steps.metadata.outcome }}
        run: test "$RUN_OUTCOME" = success
