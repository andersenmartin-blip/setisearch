name: LS7AA pointing-consistency study

on:
  push:
    branches: [m43-support-qualification]
    paths:
      - '.github/workflows/ls7aa_pointing_consistency.yml'

permissions:
  contents: write

jobs:
  run:
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python -m pip install numpy==2.3.5
      - name: Evaluate frozen LS7AA
        run: python scripts/ls7aa_pointing_consistency.py
      - name: Audit LS7AA independently
        run: python scripts/ls7aa_pointing_consistency_audit.py
      - name: Rebuild manifest and publish
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          import hashlib
          from pathlib import Path
          out=Path('results_ls7aa_pointing_consistency')
          files=sorted(p for p in out.iterdir() if p.is_file() and p.name!='SHA256SUMS')
          (out/'SHA256SUMS').write_text(''.join(f"{hashlib.sha256(p.read_bytes()).hexdigest()}  {p.name}\n" for p in files))
          PY
          git add results_ls7aa_pointing_consistency
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git commit -m 'Publish audited LS7AA pointing-consistency result'
          git push origin HEAD:m43-support-qualification
