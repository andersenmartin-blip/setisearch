# M43AA: retrospective native response and stage-transition diagnostic

This is the integrated follow-up requested by M43Z_FAILURE_INTERPRETATION.md.
Selection uses the completed M43Z results and is explicitly retrospective.
It changes no detector, OFF window, 5.5 floor, rank cut, association tolerance or
acceptance gate. It provides no independent validation, additional observing
coverage, astronomical candidate or physical false-alarm probability.

## Scope fixed before execution

The machine-readable config contains every input and every probe coordinate.
Compare OFF losses 324/336/346 with the matched distributed signal-only,
strong-near-OFF and far-OFF inputs. Add the moderate OFF component alone and
the corresponding matched ON-OFF controls to assess shape differences.
Compare aggregation losses 171, 260/261/265 and 281 with their existing
signal-only and interferer-only components. Replay residual configurations
6/16/96, preserving the distinction between seven labels and three native
inventories, and a separate uninjected baseline.

Probe all retained truth-associated coordinates from each selected historical
signal case, all combined-policy surviving coordinates of the three residual
controls, and the nominal truth coordinate at every existing filter width.
Use the union within each comparison family for all of its members. Control
activity follows actual retained members. Baseline uses the union of all probes.
Inputs, repeated coordinates and endpoints are correlated within one sequence.

## Measurements

Replay the unchanged M43Z pipeline and all four existing policy decisions.
Verify each historical reference audit, confirmation evidence and full policy
decision list exactly, and independently restore the sealed null inventory and
threshold without generating or recalculating calibration rows. Validate all
96 original arrays and 48 native gathers through the existing anchor path.

At each distinct probed template/carrier, save ON and OFF response vectors over
an inclusive +/-160 proxy-bin neighborhood for every existing filter width and
all three epochs, together with uninjected baseline vectors. Record first
maximum, its contiguous half-height lobe relative to zero, and boundary censoring.
These are descriptive filtered-response widths, not fitted physical linewidths.
Record the same quantities for injected-minus-baseline responses. Baseline
subtraction is an explanatory intervention unavailable for unknown sky signals.
Record unshifted mean-subtracted response correlations for epoch pairs and paired
ON/OFF; constant vectors have undefined correlation. No correlation cut is selected.

At each central coordinate and width independently sum explicit native windows,
reconstructing full point/sinc profiles in original float32 addition order and
checking profile receipt hashes. Save per-row filtered scores, native indices,
and the fraction of each injected component falling inside each window. Require
bitwise equality against the frozen score store. Repeated scalar checks are
arithmetic validation, not independent observations.

Record isolation-mask seeds and support, retention state, retained-OFF track
evidence, exact-coordinate adjacent-OFF evidence, receiver-alias annotations,
rank evidence and all four final policy decisions. Trace all retained associated
members, including vetoed narrow ones; do not infer component attribution solely
from the final broad member's geometric association.

## Interpretation and next decision

Report which response features contain apparent separation information and where
desired signal and interference overlap. Publish negative or ambiguous outcomes.
Any suggested response-matching endpoint requires a new public prospective
freeze, explicit signal/control false-veto comparisons and evaluation beyond this
selected diagnostic panel. Preserve M43X/Z losses, original denominators, M43T
history and LS results. Any future fresh null rows exclude all 1,792 prior rows.

Ongoing owner authorization covers publication on m43-support-qualification
and README updates on main. No routine permission request or delegation.
