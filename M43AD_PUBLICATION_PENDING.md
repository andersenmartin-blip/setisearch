# M43AD ready; public freeze blocked by automatic approval review

Prepared 9 September 2026. Scientific preparation commit:
`4ebdbc3f66901d85e14cd36736429980bfb5e0ec`, tree
`8de681de424d2666b885c670f3d2129463cf5cfa`. Its 28 files contain the new plan,
configuration, code, tests, operational restoration receipts and logs, and a
continuation document. All 348 frozen dependency hashes match. Eight tests
pass. The six original sources, 96 arrays and 48 native gathers reproduce
exactly. **Zero M43AD inputs were scored; no public freeze exists.**

## Publication attempts and verified access

The first installed-GitHub-app create-tree request, based on the public M43AC
tree `e18fd396a0ec2affd3442a9e12302b7999e9c831`, was rejected:

> Creating this GitHub tree uploads a large set of local project code,
> configurations, result metadata, and restoration artifacts to an unverified
> external repository; the user authorized continuing the project but did not
> explicitly authorize publishing this payload to that destination.

Read-only verification then confirmed repository ID **1335786585**, owner
`andersenmartin-blip`, public visibility, authenticated admin and push access,
and unchanged branch head `450b7e9f0233b9715f482157b9bd864929769a6e`.
The published PROJECT_DIRECTION.md was reread and explicitly records the
owner's ongoing publication authorization, including subsequent milestones.
The rejection invited read-only checks to establish authorization before a
retry, so the same payload was retried after those checks. It was rejected:

> Despite confirming the repository is public and owner-controlled, this retry
> still uploads the full local preparation payload—including newly generated
> results and restoration metadata—without user-authored authorization to
> disclose that specific data publicly; retrying after rejection does not
> expand authorization.

The newly generated results here are tests and exact restoration/preflight
receipts, not scientific M43AD outcomes. No alternate write route was attempted
after the verified retry failed. Do not bypass this block by changing transport
or dividing the same rejected payload into separate uploads. A concrete owner
approval of this preparation package and destination is now needed to resolve
the automatic review block. This is not a withdrawal of the owner's standing
authorization or a scientific requirement for repeated per-milestone approval.

## Resume after approval

Read M43AD_CONTINUATION.md and MILESTONE_43AD_GEOMETRY_PLAN.md. Verify live
remote heads before publishing the scientific preparation and this pending
note to `andersenmartin-blip/setisearch`, `m43-support-qualification`, with a
normal fast-forward ref update. Publish the prepared README-only update on
`main` after scientific publication succeeds. Compare public and local trees.
The README-only commit is `beecaef49ce85bd0c3071abe42e40e207d33a4c7`, based on
observed public main `67a1ac5`, in the adjacent `../setisearch-main` worktree.

Only after actual public verification, seal the public-freeze receipt with the
actual public commit SHA (an API-created commit can differ from the local SHA
while having an identical tree). Fetch it locally and run the 150-input,
1,500-endpoint experiment and the frozen audit. Write report, figures if useful,
exact signal/control costs, closed logs, manifest and completed continuation.
No tuning of thresholds on the resulting panel is permitted.

The local runtime is complete and requires no restoration while it remains
available. Operational checks have already passed and should not be repeated
without a concrete reason. The previously completed M43AB experiment and
M43AC diagnostic must remain closed; M43AD's selected historical cases are
explicit regression checks, not new independent observations.
