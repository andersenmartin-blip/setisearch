# M43N — exhaustive scores on the remaining two real epoch pairs

M43M completed four widened sources for epoch2_on/off and epoch3_on/off at
m37_1412p5. Qualify the unchanged M43I native filtering and integrated gather
for these four sources, every width 1,3,5,9,17,33,65,129, all 1,701 fixed bank
templates and every 747,793 support carrier (including 64 guards on each side).
The endpoint is 32 source/width checks, 1,728 batches, 54,432 full vectors and
40,703,868,576 integrated cells. Native reference coverage additionally spans
all 579,705,984 valid native values in 512 row/width checks.

Freeze code, tests, source receipts, bank/factors/grid, runtime and protocol
publicly before new numerical evaluation. Source IDs come only from the
retained M43M result, not from arbitrary local self-sealed receipts. Scan
factor matrices are fixed by the same M43E table. No spectral downloads.

Production uses unchanged M43I source/cache/gather code. Independently indexed
native windows and absolute-coordinate integration use the unchanged M43L
factorized oracle. Compare all valid native values directly including dtype,
then compare row and payload hashes. Confirm whole-bank geometric coverage
with the existing M43K endpoint bound. Compare every integrated cell using
M43L's tested batch routine: 32 templates per batch, 4,096-carrier production
and reference chunks, 2,048-center native reference chunks. The final batch
contains five templates. Width one is included and tested explicitly.

This extends the scope of established numerical methods to additional real
inputs. There is no pre-existing M43I/K score digest for these new sources;
do not fabricate ancestor overlap. Both implementations share the intended
formula, input factors and NumPy runtime. Equality is numerical validation,
not independent scientific replication.

Run at most seven ordinary subprocesses under the eight-CPU/20-GiB environment;
schedule source/width jobs in descending width order, with frozen source order.
Use the tested M43L file stop marker, no manager sockets or AI agents. Each
job retains one source, cache, independent native reference and one bounded
32-template score batch. The 512-MiB adapter bound excludes caller/reference,
process and OS memory; it is not a combined-job RSS cap.

Publish sealed source/width checkpoints after native checks and every completed
batch; completion requires every batch. A peer failure stops queued jobs and
running jobs at their next batch boundary. Preserve failures and partial files;
never count partial inventory as success. Reruns recompute rather than trust
self-sealed checkpoints to skip evaluation. Publish completed checkpoints during
long execution and audit the exact 32-product inventory before the final result.

M43J/L retain their original first-pair denominators. Combined full integrated
coverage after success is 61,055,802,864 cells across six sources at eight widths
and one window. These are correlated numerical comparisons, not independent
trials, candidates or sensitivity estimates. Multi-epoch stack/detection logic
and fresh null/injection calibration require separately frozen later endpoints.
