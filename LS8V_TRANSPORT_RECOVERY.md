# LS8V — bounded transport-only recovery

21 September 2026. The first science attempt at freeze
`b2b185f8a592e8ae465fa396e96be746e76ecf4c` stopped during the TLS handshake
for the first DEFAULT-L2 download URL request. The first result is preserved
unchanged at `d6703a70572eb70c0e028808c8be9e20c73de63e` under
`results_ls8v_l2_screen/`. The log identifies `get_url`, before the table
range request. Its manifest contains only run status, environment, the two
passing known-answer tests and the error log: no table, ledger or scientific
outcome was produced. Workflow 35622872841 ended with failure.

This separately published transport recovery retains that outcome. It does
not change the pair, original header identities, exact 24,012-byte science
scope, arithmetic, thresholds, signed clustering or independent audit.
The original public LS8V L2 protocol remains the scientific specification.

Before access, verify the original failure inventory/checksums and pins of
the original adapters, stable scorer, independent auditor, report, metadata
and L2 scope. Output only into new `results_ls8v_l2_recovered/`; refuse to
overwrite either an existing recovery or the first attempt.

For each exact product, allow at most three URL-resolution attempts using
the original 45-second timeout and original request, separated by five
seconds. Retry only a TimeoutError (including an URLError whose underlying
reason is TimeoutError). Record key, attempt, UTC time, outcome and error
type; never record temporary URLs. Other errors stop immediately. The
science-table HTTP request is unchanged and is not retried by this wrapper.
Its exact range, If-Match, ETag, size and filename checks remain mandatory.
No alternate endpoint, product, aperture or visit is used.

Run the two existing known-answer tests before values. After acquisition,
invoke unchanged pinned scoring, scalar audit and report using the new
output directory. Preserve all signed outcomes and transport attempts,
including an incomplete recovery on failure. Publish the full result and
checksums. A successful recovery must not relabel the first run as success.
The zero-crossing/all-signed-image-follow-up branches in the original
protocol remain in force. Publication is already authorized.
