# LS7I continuation

Input restoration is prepared to start the approved two-week plan. Publish
LS7I_INPUT_FREEZE.sha256 and the new workflow, then complete its acquisition,
reconstruction and independent raw-FITS/recipe audit. The workflow publishes
only the audited input package if the science branch still matches its source.

No new background model is evaluated in this step. Preserve all historical
results and use LS7I_INPUT_PROTOCOL.md as the exact restoration boundary.
The report replaces this note with the verified input interface and next action.
