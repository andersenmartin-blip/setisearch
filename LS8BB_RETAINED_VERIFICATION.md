# LS8BB — retained delivery verification and presentation review

23 September 2026. The image study is already complete and independently
audited at `0031edaebfcb221bd9b381fb524ace39d3f91342`. Both signed events
are CORRECTION_LINKED. There is no unresolved-set residual study to trigger.

This verification completes the overlap-consistency check declared before
pixels in LS8BB_PAIRED_IMAGE_PROTOCOL.md. It uses **retained files only**,
verifies the entire pinned image manifest and every compressed/uncompressed
receipt, and compares the 28 shared rows byte for byte for CAL, COR and
smearing. The expected contexts are P0 rows 11:40 and N0 rows 10:39; compare
P0 without its last row to N0 without its first row. The unique union is
30 rows per product, not 58 independent rows. No archive bytes, native fits,
screening, thresholds or classifications are added or repeated.

The same saved event maps also receive a combined presentation figure with
explicit subplot spacing. This addresses crowded axis labels in the second
original figure. Preserve the exact original per-event common signed color
scale, finite footprint, centers, apertures and values. The original figures
remain unchanged. No plot feeds any scientific decision.

All 166 existing scientific files retain their public identities. The local
Git refresh was unavailable; approved connector reads verified 162 local
files against their public blobs. Four large compressed CAL/COR ranges are
above the contents endpoint's payload limit and remain fully verified by the
published independent image audit. The optional artifact was uploaded
successfully, but its returned download URL gave HTTP 403 in the terminal;
no successful local ZIP checksum is claimed. This retained CI verification
provides the exact overlap check without new acquisition or native reruns.

The verification script pins the public image manifest and exact image scope.
Require its public freeze HEAD and clean tracked tree. Preserve summary,
receipt hashes, overlap hashes, layout-only figure, log, environment, status
and checksums, including failures. Any verification failure stops closure
without modifying inputs or the original scientific result.

After successful verification and visual review, close LS8BA–LS8BB and prepare
LS8BC for rank-22 GJ 422, exact pair CH_PR100018_TG012201_V0300 /
CH_PR100018_TG012202_V0300, under separate own-header and L2 freezes.
Prior labels and closed studies remain unchanged. Calibration NOT_READY;
request UNSENT. Publication authorized; delegation deferred.
